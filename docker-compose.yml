version: '3.8'

services:
  fivem:
    build: .
    container_name: fivem_server
    restart: unless-stopped
    
    # ---------------------------------------------------------
    # PORT MAPPING STRATEGY
    # ---------------------------------------------------------
    ports:
      # DIRECT MAP: Bypasses Traefik. Essential for Game Traffic.
      - "30120:30120/tcp"
      - "30120:30120/udp"
    
    expose:
      # INTERNAL ONLY: Traefik will route domain traffic here.
      - "40120"

    environment:
      - FIVEM_LICENSE_KEY=${FIVEM_LICENSE_KEY}
      - TXADMIN_PORT=40120
      # Database connection vars
      - DB_HOST=mariadb
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      
    volumes:
      - fivem_data:/opt/fivem
    
    depends_on:
      - mariadb

    # ---------------------------------------------------------
    # TRAEFIK CONFIGURATION (COOLIFY)
    # ---------------------------------------------------------
    labels:
      - "traefik.enable=true"
      # Tells Traefik to route HTTP traffic to the Admin port (40120), NOT the game port
      - "traefik.http.services.fivem.loadbalancer.server.port=40120"
      # (Optional) If you want to force HTTPS redirection:
      - "traefik.http.routers.fivem.middlewares=redirect-to-https"

  mariadb:
    image: mariadb:11.4
    container_name: fivem_database
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MARIADB_DATABASE: ${DB_NAME}
      MARIADB_USER: ${DB_USER}
      MARIADB_PASSWORD: ${DB_PASSWORD}
    volumes:
      - mariadb_data:/var/lib/mysql

volumes:
  fivem_data:
  mariadb_data: